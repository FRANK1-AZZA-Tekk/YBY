# Observabilidade

Cada interação recebe um `correlation_id`. Registrar latência de STT, roteamento, primeiro token, primeiro áudio, duração total, fallback, provider, modelo, custo estimado e conclusão.

O áudio bruto não deve ser salvo por padrão.
